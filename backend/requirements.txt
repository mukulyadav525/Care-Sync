# Version floors kept modest (not exact pins) so this installs on whatever
# Python is actually available on a given server — the previous exact/high
# pins here (and in ai/requirements.txt) assumed Python 3.12+ and failed
# outright on older servers. pip will pick the newest version compatible
# with whatever Python is running this.
Django>=4.2,<6.0
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
whitenoise>=6.6
# Data science & signal processing
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
plotly>=5.18
bokeh>=3.3
# Google APIs
gspread>=6.0
oauth2client>=4.1
google-auth>=2.25
google-auth-oauthlib>=1.2
google-api-python-client>=2.110
# PDF generation
reportlab>=4.0
# Utilities
chardet>=5.2
Pillow>=10.2
requests>=2.31
