# AI agentic pipeline service (FastAPI, standalone from the Django backend)
fastapi==0.139.0
uvicorn==0.49.0
pydantic==2.13.4
numpy==2.5.0
pandas==3.0.3
scipy==1.18.0
python-dotenv==1.2.2
langchain-core==1.4.9
langgraph==1.2.7
langchain-xai==1.2.2

# ---- Optional: only needed once a real trained checkpoint is wired up via
# HRV_MODEL_DIR (see ai/hrv_forecast/model_loader.py and
# docs/HRV_INTEGRATION.md). The /hrv/* endpoints work today without these,
# serving the deterministic mock engine.
# torch>=2.4
# joblib>=1.4
# xgboost>=2.1
# scikit-learn>=1.5
