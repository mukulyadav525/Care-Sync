# AI agentic pipeline service (FastAPI, standalone from the Django backend)
#
# numpy/pandas/scipy are left unpinned (just a floor version) because their
# exact-pin releases require a very recent Python (3.12+). Pinning them tight
# broke installs on servers with older Python — pip will pick the newest
# version that's actually compatible with whatever Python is running this.
fastapi==0.139.0
uvicorn==0.49.0
pydantic==2.13.4
numpy>=1.24
pandas>=2.0
scipy>=1.10
python-dotenv==1.2.2
langchain-core==1.4.9
langgraph==1.2.7
langchain-xai==1.2.2

# ---- Optional: only needed once a real trained checkpoint is wired up via
# HRV_MODEL_DIR (see ai/hrv_forecast/model_loader.py and
# docs/HRV_INTEGRATION.md). The /hrv/* endpoints work today without these,
# serving the deterministic mock engine.
# torch>=2.4
# joblib>=1.4
# xgboost>=2.1
# scikit-learn>=1.5
